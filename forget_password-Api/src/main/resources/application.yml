spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/emp
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port : 9090

